## DCMMC's Blog

Powered by `Hexo` and theme `hexo-theme-matery`

## 运行与部署

```bash
$ hexo g && hexo s
```

部署:

```bash
$ hexo d
```

> 可能需要生成一下 github 的 RSA key

## TODOs

* [ ] `\_posts` 中的 `assets` 问题, 写一个小插件
* [ ] 用 CI 进行自动部署
